[server]
host = 'localhost'
port = 8000

[frontend]
host = 'localhost'
port = 5173

[session]
type = ''   # 'paseto' or 'express'
secret = '' # Session cookie signing key (only required for express session)

[database]
host = 'localhost'
port = 5432
name = ''
user = ''
password = ''

[maintenance]
adminProfile = ''